export enum AnswerChoice {
  'Single' = 0,
  'Multiple' = 1,
  'Open' = 2
}
export const AnswerChoiceOptions = new Map([
  [AnswerChoice.Single, 'Single'],
  [AnswerChoice.Multiple, 'Multiple'],
  [AnswerChoice.Open, 'Open'],
]);
